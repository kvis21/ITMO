- webpack vs react-scripts
- ant vs gradle
- не работает xml команда
- не знает, как работает команда report
- source map с примером
- alt регулярка + рефактор скрипта (убрать код из глобальной области)
- генерить манифест до сборки архива
- нормальнкая реализация alt
- откуда берутся зависимости и что такое implementation, runtimeOnly и т.д.
- осознать как работает команда team